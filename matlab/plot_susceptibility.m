function plot_susceptibility(varargin)
%PLOT_SUSCEPTIBILITY Backward-compatible wrapper.
%
% Prefer plot_susceptibility_qdep for q-dependent susceptibility maps.

plot_susceptibility_qdep(varargin{:});
end
